enum INTERFACE {
	INTREFACE_PUSH,
	INTERFACE_POP,
	INTERFACE_FIND,
	INTERFACE_SAVE,
	INTERFACE_LOAD,
	INTERFACE_CLEAR,
	INTERFACE_END,
	INTERFACE_TOTAL
};

void menu();
void push();
void pop();
void find();
void save();
void load();
void clear();
